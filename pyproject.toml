[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mbb_model"
version = "0.0.1"

dependencies = [
"matplotlib",
"astropy",
"numpy",
"scipy",
"emcee",
"re",
"multiprocessing",
"corner"
]